from setuptools import setup

setup(
    name = "regtemplate",
    version = "1",
    description = "Parse templates into regexes, and use those to match text into name-value pairs",
    license = 'MIT',
    author = 'Magnus Jeffs Tovslid',
    packages = ['regtemplate'],
)